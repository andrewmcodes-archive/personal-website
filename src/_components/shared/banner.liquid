---
name: Banner
description: Displays text at the top of the page. Should be used as an announcement.
variables:
  message:
    - string
    - The base message on the banner
  full_message:
    - string
    - The full message on the banner
  action: with block
---
<div class="sticky inset-x-0 top-0 z-50">
  <div class="px-2 mx-auto bg-blue-600 shadow-lg sm:px-6 lg:px-8">
    <div class="p-2 sm:p-3">
      <div class="flex flex-wrap items-center justify-between">
        <div class="flex items-center flex-1 w-0">
          <span class="flex p-2 bg-blue-800 rounded-lg">
            <svg class="w-6 h-6 text-white" fill="none" viewBox="0 0 24 24" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M11 5.882V19.24a1.76 1.76 0 01-3.417.592l-2.147-6.15M18 13a3 3 0 100-6M5.436 13.683A4.001 4.001 0 017 6h1.832c4.1 0 7.625-1.234 9.168-3v14c-1.543-1.766-5.067-3-9.168-3H7a3.988 3.988 0 01-1.564-.317z"/>
            </svg>
          </span>
          <p class="mt-0 ml-3 font-medium text-white truncate">
            <span class="md:hidden">
              {{message}}
            </span>
            <span class="hidden md:inline">
              {{ full_message }}
            </span>
          </p>
        </div>
        {% if action %}
          <div class="flex-shrink-0 order-3 hidden w-auto mt-2 md:block sm:order-2 sm:mt-0">
            {{ action }}
          </div>
        {% endif %}
      </div>
    </div>
  </div>
</div>

<nav class="z-20 p-4 shadow-md bg-gray-1000" id="topnav">
  <div class="container flex flex-wrap items-center justify-between mx-auto">
    <div class="flex flex-shrink-0 mr-6">
      <a class="text-xl font-semibold text-red-500 hover:text-red-600" href="/">
        Andrew Mason
      </a>
    </div>
    <div class="block lg:hidden">
      <button class="flex items-center px-3 py-2 text-red-500 border border-red-500 rounded navbar-burger">
        <svg class="w-3 h-3 fill-current" viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg">
          <title>Menu</title>
          <path d="M0 3h20v2H0V3zm0 6h20v2H0V9zm0 6h20v2H0v-2z"></path>
        </svg>
      </button>
    </div>
    <div class="hidden w-full navbar-menu lg:flex lg:flex-grow lg:items-center lg:w-auto">
      <div class="lg:ml-auto">
        {% for item in navigation %}
        <a class="block mt-4 mr-10 text-gray-200 lg:inline-block lg:mt-0 hover:text-white" href="{{ item.url }}">
          {{ item.name }}
        </a>
        {% endfor %}
      </div>
      <div>
        <a class="inline-block px-6 py-3 mt-4 leading-none text-white bg-red-500 rounded shadow lg:mt-0 hover:bg-red-600"
          href="{{ primary.url }}">
          {{ primary.name }}
        </a>
      </div>
    </div>
  </div>
</nav>
